# v0.1.0

Initial release. See [CHANGELOG.md](../../CHANGELOG.md#010--2026-07-10)
for the full capability list.

Highlights: ~90 resource methods across Identity, Authentication,
Authorization, and Administration, all verified against the live Qeet ID
backend; zero third-party dependencies; local JWKS session verification;
auto-pagination; automatic retry; HMAC webhook verification; zero-config
OIDC discovery.
